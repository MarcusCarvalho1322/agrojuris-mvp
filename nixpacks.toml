[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["cd mvp_backend && pip install -r requirements.txt"]

[start]
cmd = "cd mvp_backend && uvicorn app:app --host 0.0.0.0 --port ${PORT:-8000}"
